#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../grunt-requirejs/bin/grunt-requirejs" "$@"
  ret=$?
else 
  node  "$basedir/../grunt-requirejs/bin/grunt-requirejs" "$@"
  ret=$?
fi
exit $ret

#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../grunt-less/bin/grunt-less" "$@"
  ret=$?
else 
  node  "$basedir/../grunt-less/bin/grunt-less" "$@"
  ret=$?
fi
exit $ret
